<form method="post" action="{{ path('app_special_actor_delete', {'id': special_actor.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ special_actor.id) }}">
    <button class="btn">Delete</button>
</form>
