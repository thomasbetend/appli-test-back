{% extends 'base.html.twig' %}

{% block title %}SpecialActor{% endblock %}

{% block body %}
    <h1>SpecialActor</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ special_actor.id }}</td>
            </tr>
            <tr>
                <th>Firstname</th>
                <td>{{ special_actor.firstname }}</td>
            </tr>
            <tr>
                <th>Lastname</th>
                <td>{{ special_actor.lastname }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_special_actor_index') }}">back to list</a>

    <a href="{{ path('app_special_actor_edit', {'id': special_actor.id}) }}">edit</a>

    {{ include('special_actor/_delete_form.html.twig') }}
{% endblock %}
