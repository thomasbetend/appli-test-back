<form method="post" action="{{ path('app_user_special_delete', {'id': user_special.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user_special.id) }}">
    <button class="btn">Delete</button>
</form>
